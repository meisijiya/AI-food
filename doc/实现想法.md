


# 为每个推荐记录新增文本输入功能，用于记录对打卡美食照片的评价，前后端一起实现，前端要统一风格。

# 可以分享图片，动态生成图片，生成的图片跟分享链接的页面UI类似但是要少个App入口的部分，增加二维码的首页链接供新用户加入；动态生成图片是由后端生成还是前端生成呢好呢；

# 现在实现类似于博客发布的功能【将推荐记录发布在大厅】，发布的推荐记录所有用户都可以查看，推荐记录的收集信息可以用作筛选功能；发布的记录在前端的布局应该是瀑布流布局类似于小红书的布局结构，如何每个发布记录的首页展示要有打卡照片（没有打卡照片就不留空照片的地方），推荐食物名、评论的前30个字、用户名、用户头像；可以对发布的推荐记录进行点赞，用Redis实现点赞的缓存于计数器、存储点赞数、未读通知。用redission实现分布式锁来防止高并发场景下的点赞数据不一致；用户打开推荐记录可以在记录的最下边进行评论，评论面向所有人可见，每条评论都要对发布者的未读通知+1，评论都是展示最新的，通过分页展示，旧的评论在最后一页；


# 实现好友关注、粉丝功能，在用户界面可以看到自己关注的列表和粉丝列表；发布的推荐记录可以选择公开发布（发布到大厅以及好友榜中也能看到）或者发布给朋友（仅发布给好友榜中）；每个用户访问大厅中发布的推荐记录，在对应推荐记录的Redis中的数据结构进行计数加一，热榜数据结构选择sorted set，实现热度排行榜（最多20条），在大厅页面的顶部有个副导航栏，里面有热榜（热度排行榜）；副导航栏里面还有友榜（好友的发布推荐的列表），每次好友发布推荐，，这个Redis最多存储100条好友的推荐记录（先进先出，不管看没看），Redis存储的是和大厅首页的数据（尽量少占内存），详细的数据在用户点击后才会重数据库中加载；

# 实现关注、互关功能；实现聊天功能，使用双端口架构；当用户点击到聊天页面中，开始拉取未读的最新消息以及未读消息数（应当存在Redis）；有好友对话列表，通讯录列表（互关好友）；打开聊天窗口拉取聊天记录，实现实时消息推送；


# 新增搜索用户，支持模糊查询，按连续签到的天数排序，每次查询十条（分页查询），下拉刷新更多；发布推荐页详情中无法关注发布者；在前端中的大厅大字标题旁边新增好友页面入口，将大厅页面的副导航栏的大厅删除，点击大厅大字即可返回到大厅；点击好友页面进入到顶部有聊天列表、私聊（点击会打开上一次聊天的对象）、好友列表，三个页面的展示，作为副导航栏；然后三个功能的前端实现参考sanctuary-UI的技能；

# 完善用户信息页，用户可以编辑用户信息，上传头像（复用图像上传工具类），也是展示缩略图，点击图片才会显示原图，所有需要展示头像的地方都要展示，缓存到前端，设置有效期（半个钟+随机时间，防止缓存雪崩）； 副导航栏点击私聊应当立即打开聊天会话，默认上一次聊天会话，没有则显示提示词（提示开始欢快聊天之类的）；聊天页的对话要有用户头像；

# Token信息保存到Redis中吧，过期时间设置3天，用户每次登录自动续期；搜索用户时前端报错：

# 全局的照片上传功能，若要替换（如推荐记录打卡上传、头像上传）都要对应的删除旧的照片（包括缩略图和原图）

# 热榜数据详细应该保存到Redis中，每一个钟才会根据Redis中的浏览次数重新将新的最热20条从MySQL加载到Redis中，直接覆盖旧的数据；

# 只有双方互相关注才可以发送消息，否则至多只能发送五条（粉丝可发五条，被关注的无法发送消息，只能查看）；个人中心的头像图片、聊天的头像图片要缓存在前端，设置有效期（跟之前设置的统一）；数据库的操作要有事务性，要能够保证删除推荐页也要同步删除发布推荐记录以及照片等相关内容，要么同时成功要么同时失败；

# 完善消息通知功能，点击消息通知后的页面，需要完善的点如下：用户打开消息列表时，每次从数据库分页查询10条十条查询，然后相对应的扣除Redis中的未读通知数10条；消息通知中的用户的公开推荐记录中的评论会显示一条条的；可以删除消息通知记录，或者一键清除；每个记录应当有对应公布记录的跳转功能，点击后跳转到对应的公开推荐记录的详细页面；粉丝或好友的聊天信息则汇总一条，展示最新的消息，然后显示条数；消息通知的数据都存储在Redis中，然后尽可能少的保存数据，过期时间为7天；

# 优化消息通知，每次加载大厅页面应当也要拉去消息通知的未读消息数；消息通知清空按钮的弹窗-前端需要优化；

# 丰富聊天的功能，实现照片的收发（复用图像上传工具类，聊天产生的照片单独一个表，图片仅保存30天【采用定时任务删除】，请求都是缩略图，点击图片才会显示原图），实现文件收发（也是单独一张表来记录，文件仅保存30天，且仅支持50M以下大小的文件）；实现清除聊天对话记录（只有两方都选择删除了，或者两方都不在互相关注对方），满足以上两点其一，则对应的删除聊天记录所对应的文件、图片（软删除，硬删除交给定时任务）；



# 保持风格一致的前提下，电脑端的我的页面、记录页面要左右两边留白，不要完全贴在边边，整体内容占五分之三左右即可，整体居中；电脑端的大厅页面的首页推荐记录的照片要规定长宽，采取主流的大小，不要太大了；无论电脑端还是手机端，首页推荐的记录的照片要获取的是缩略图，然后点击打开详细的公开推荐显示的是完整的图片；

# 聊天页面的文件和照片功能放入到一个加号中点击后展开，展开的UI是在顶层图层上然后在聊天输入框上面，带点毛玻璃的质感，参考sanctuary-UI技能即可；Emoji的点击后的展示也是在顶层图层上，同时在也在输入框上边，带点毛玻璃的质感，参考sanctuary-UI技能即可；这两个新的UI都是能够随着屏幕移动的，而不是停留在远处，且始终底部贴着聊天框；

# 电脑端的大厅应当一行可以放四列；自己的用户信息应当全局缓存，头像图片也要缓存到，当发送评论的时候，图片和用户信息要立马回显出来，而不是要刷新页面后才回显；聊天页面的Emoji的设计也要同步到评论中，评论区也可以上传图像；

# 热榜、关注榜、聊天列表、好友列表、推荐记录的电脑端也都设置为屏幕的五分之三且居中；评论的照片和表情包功能打不开；电脑端的表情栏只能显示一行，间距缩小以显示更多；

# 电脑端的个人中心也优化一下布局，各个部分采用瀑布布局进行显示，像是俄罗斯方块一样；在此布局下，签到部分不要那么大了，太大了；首页的推荐页提醒，点击后显示的推荐记录确保要跟数据库的强一致，所以Redis存的是主键（数据库表中对应的那个推荐记录数据），然后点击从MySQL数据库查询数据，每次完成拍照打开就拿该数据的主键和Redis中的比较【这里的主键应当在查询所有数据的时候就包含了，不要多次查询数据库，尽量压低查询次数，以免拖累数据库】，一样就删掉Redis中的主键记录，然后首页不显示（为空则不显示，有主键则显示提醒）；

# 任何时候访问到被删除的记录时候或者不存在的资源的时候，前端应当通过catch异常然后弹窗报错而不是直接重定向到登录页；删除公布的推荐记录，要把相应的评论删除、图片删除，设置定时任务进行硬删除；

# 用户评论的时候，评论的头像和名称应当立即从缓存中读取，立即回显，而不是显示空头像和名称，用户的信息是要全局缓存的（用户头像、名称）；

# 现在仔细检查项目在开发环境是否会有sessionId泄露风险，要确保项目安全，看看有没有什么措施加强；

# 详细检查项目是否会有内存泄露的风险，是否会有数据堆积（永远无法访问且不会被定时任务删除的资源），要确保项目的健壮性，看看有没有什么措施加强和杜绝此类情况；

# 消息通知的清空功能的弹窗丢失CSS样式，请详细检测该功能的代码，确保能够正常渲染；

# 完善推荐记录详情页的UI设计，不要黑色的背景，要使用浅色的背景,其他就再美化一下；

# 帮我审计好友功能的代码，我调试发现我对好友列表发起私聊，聊天列表不会显示；打开聊天会话不会加载历史记录；聊天会话的清除历史记录现在功能无法正常运行，以及我希望聊天会话的清除历史记录的功能逻辑有待完善，我想要更加健壮的实现方式，确保删除聊天记录不会影响用户体验；

# 再进一步优化聊天功能，现在用户A清除聊天记录，然后当用户B又发消息，用户A删除的数据还是会显示，看看怎么优化，既能够删除，也能保证用户体验又能确保数据能够最终被硬删除；

# 发送的消息对应的给Redis计数加一，以便在消息通知显示的时候能知道有多少条未读消息（同评论、点赞的未读消息一起计数），每当用户删除单个或者多个消息通知记录则对应删除Redis中的计数；

# 聊天功能有 bug，给关注的人发送后，再进入会话会消失，然后互相关注后都无法看到消息，以及发新的消息双方都看不到

# 热榜和关注榜有 bug，删除的推荐奖励还会留在榜上（是不是因为代码逻辑redis 不是即时删除，如果不是即使删除，前端提示要显示帖子消失咯的友好提示），你帮我审查一下，问题大不大，看看有没有必要稍微优化一下

# 个人信息修改页的头像上传新的后无法回显；登录改用邮箱登录，然后用户名改成用户ID，是为唯一标识，不能修改，用户昵称，可修改，昵称可重复，用户名不可重复；

# 新增发布给粉丝而不发布在大厅中的功能，点击发布可以选择发布给粉丝或者发布到大厅；发布给粉丝的，粉丝可以在大厅中（会带有仅粉丝可见的标识）、关注榜中查看；发布到大厅的则大厅中、热榜、关注榜都可查看；

# 完善一个请求多次请求到数据库（看看有没有能够用到批处理的，如果可以就优化一下，减少IO次数）

# 聊天室的文件和图片应当保存在chat_file和chat_photo表中，不是都保存在chat_message表中；而且前端显示照片应该直接显示缩略图，而不是显示不出来而且不应该在会话列表中显示出照片的请求路径（用【照片】来代替消息）；

# 后端出现异常会暴露一些敏感信息（如数据库、Redis的IP地址和端口号）给前端，你看看哪里有，完善一下

# 前端点击私聊按钮，好像跳转了两次，快速显示了两个页面，看看哪个view多余了

# 文件传输的提示信息要明显（当文件大于50M时候，要提示用户文件过大）；聊天室中的聊天内容可以点击后显示复制和删除按钮，文件和图片的左侧常驻删除按钮（在文件表和照片表中新增is_receiver_delete和is_sender_delete字段，当两个字段都为true，则异步执行删除该字段的命令以及对应的删除文件或图片）

# 优化点赞功能的处理逻辑，前端发送点赞请求到后端服务器，后端用 Lua 脚本检查并写入 Redis，后端发送点赞事件到消息队列，消息队列异步写入数据库；消息队列看看是采用Redis模拟还是利用框架来实现；系统用时间片来分散压力，避免瞬间高并发；用 HeavyKeeper 算法探测热点，本地缓存热门数据（Caffeine）

# 完善一个并发测试，看看项目是否能够支持并发测试

#  设计一个用户图像相似度比较器，通过对参数有某些有规律的计算方式将n个参数（每次的参数量不固定）进行多次哈希函数取值，类似于布隆过滤器一样的，然后这个bit数组与别的用户的bit数组取与，看看命中多少，显示相似度。这个bit数组要一直持久化在Redis中，为了防止内存丢失，定时持久化到MySQL中。这个相似度比较器用来在显示用户之间的相似程度；就用最新的十个推荐记录进行统计，每次来新的就把最晚的一个删除，然后重新统计（先进先出）；参数是推荐记录的收集信息；

